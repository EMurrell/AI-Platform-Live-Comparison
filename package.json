{
  "name": "u7-ai-platform-comparison",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test",
    "refresh": "node scripts/refresh.mjs",
    "check": "npm run build && npm test"
  }
}
